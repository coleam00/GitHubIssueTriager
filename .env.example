DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
OPENAI_API_KEY=sk-...
GITHUB_REPO=coleam00/claude-memory-compiler

# Neon branching for parallel worktrees (optional — worktree hooks consume these)
NEON_API_KEY=
NEON_PROJECT_ID=

# Anthropic key only needed if running the Archon adversarial-dev workflow locally
ANTHROPIC_API_KEY=
